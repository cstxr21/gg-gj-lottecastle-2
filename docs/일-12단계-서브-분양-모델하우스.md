> 🔒 **완료 게이트**: 이 단계를 "완료"라 말하기 전 `npm run verify -- --stage=12` 가 **전부 GREEN**임을 출력으로 증명한다(게이트 규약·auto/manual·필터 주의 = [0-시작하기](일-0-시작하기.md) §게이트).

# 12단계 — 서브페이지: 분양안내 · 모델하우스

> **작업 순서 12/16** · 전제: 5·6·7·8단계 · 자기완결(IA 각 페이지 + UI §2.7~2.8 원문 포함)
> **읽을 것(착수 전)**: [3단계](일-3단계-검증보강.md) 확정 fact · [6단계](일-6단계-전역규칙.md) §C·§F · [8단계](일-8단계-공통컴포넌트.md) 폼·§9 `SourceNote` · [11단계](일-11단계-서브-단지정보.md) §내부링크 시스템 · [9단계](일-9단계-관심고객등록.md) 폼 백엔드
> **대상 2개**: 분양안내 `/sales` · 모델하우스 `/modelhouse`
> **공통 전제**: 미확정(분양가·청약·일정·계약)은 전부 **"입주자모집공고 시점 기준 추후 안내"**, 수치 창작 0, "최저가/확정/직결" 단정·비교 금지(표시광고법), 핵심 정보 17px+ ink 500.
> **이미지·밀도 원칙**: [11단계 §공통 원칙](일-11단계-서브-단지정보.md) 동일 적용 — 큐레이션 이미지(특히 modelhouse 인테리어/유닛 갤러리) **최대 활용**(keep된 컷 전부 배치, 1~2장 방치 금지), **세로 인포그래픽/세로컷은 무크롭**(width/height intrinsic+h-auto), 이미지 임베드 명칭(유닛·평형·시설)은 **OCR 검증값만**(창작 0). **현장명 본문 밀도(대명사 환원)** 동일 적용 — 진단선 250자당 1회, 대명사 환원까지가 원칙, 횟수 채우기 0([14단계 §브랜드 토큰 본문 밀도](일-14단계-SEO-GEO.md)).
> ⚠️ **[필수 게이트] 적용**: [11단계 §공통 원칙 게이트](일-11단계-서브-단지정보.md#공통-원칙--본문-밀도이미지-활용-thin-페이지-금지) 동일 — 작성 전 카테고리 이미지 **전수 확인** → **[분류표](이미지-배치표.md) 기록** → **완료조건 항목별 ✅ 증명**. 안 본 이미지 제외·near-dup 추측 제외 금지.
> **배포 점검(16단계)**: 분양가·일정 시점 표기와 광고심의필·시행/시공 3주체 등 표시광고법 표기가 빌드에 살아있는지는 배포 직전 **[16단계](일-16단계-배포점검.md)**가 확인한다(누락 = blocker).
> **이 단계 blocker**: G12-FORM · G12-SCHEDULE

---

## 페이지별 섹션 → 콘텐츠 매핑 + 구성 순서 (영역 적합성)

| 페이지 | 구성 순서(섹션) | 들어가면 안 되는 것 |
|---|---|---|
| **분양안내 `/sales`** | 히어로 → 인트로 → 공급개요 표 → 분양가 → 청약 자격 → 분양 일정 → 계약 조건 → 사업주체 → 유의사항 → 분양상담 → About | 평면 상세(→평면도 요약+링크), 단지 조경/커뮤니티(→단지안내), ★**인지세·부동산 거래신고(신고기한·신고필증) 안내 — 내용·이미지 모두 금지** |
| **모델하우스 `/modelhouse`** | 히어로 → 2단(좌 위치·전화 / 우 예약폼) → 위치안내 표 → **유닛·인테리어 갤러리** → 관람 3STEP → 분양상담 → FAQ 링크 → About | 분양가 상세(→분양안내), 평면 상세(→평면도), 하단 관심고객등록 섹션(→메인 10단계 1곳만) |

> ⚠️ **분양가·청약·일정 상세는 `/sales`에만**(평면도·프리미엄 등 다른 페이지는 요약+링크 — near-duplicate 금지). **인테리어/견본주택 갤러리는 `/modelhouse`에만**(평면도는 텍스트 링크만, 동일 이미지 두 페이지 중복 게재 금지).

> 🚫 **분양안내에 「인지세」·「부동산 거래신고」 넣지 말 것(내용·이미지·표·PDF 캡처 전부)** — 입주자모집공고·계약안내에 있어도 **웹사이트로 옮기지 않는다**. 인지세 부담 구조·거래신고 기한(30일)·신고필증 발급 등은 **당첨 후 계약 단계에서 계약서와 함께 안내되는 절차 사항**이지, 방문자가 사이트에서 찾는 정보(분양가·청약 자격·일정·평면)가 아니다. 홈페이지 유의사항은 **분양가·일정 시점 표기 / CG 이미지 고지 / 표시광고법 3주체·광고심의필**까지만. 관련 이미지·표는 **4단계 [분류표](이미지-배치표.md)에 "미사용 — 분양안내 제외 규칙"으로 기록하고 배치하지 않는다**(이미지 최대 활용 원칙의 예외 = 규칙 위반 아님). 전사·정리하는 작업 자체가 시간낭비다.

> **내부링크 시스템 = [11단계 §내부링크 시스템(심플)](일-11단계-서브-단지정보.md) 정본 적용**: **카드 블록·매트릭스 없음**(전역 nav가 상호연결). 본문 맥락 링크 **≤2/페이지**(퍼널 앞 또는 disambiguation, 동일 URL 1회), **About 링크 0**, 하단 CTA 1개(모델하우스는 자체 예약폼), BreadcrumbList JSON-LD, orphan 0. 표준 퍼널 종착 두 페이지.

---

## 분양안내 `/sales` (모티프 `doc`)

- **TITLE** `{현장명} 분양안내 | 분양가·청약·일정` · **DESC**(≤80) `{현장명} 분양가·청약 자격·분양 일정을 입주자모집공고 기준 확인, 상담 {전화}.`
- **OG:TITLE** `{현장명} 분양 — {N}세대 · {청약자격요약} {사업유형}`
- **H1** `{현장명} 분양안내` · **H2** 공급 개요(표: 단지명·대지위치·건축규모·세대수·전용면적) · 분양가 안내 · 청약 자격 · 분양 일정 · 계약 조건 · 사업주체 · 유의사항 · 분양 상담.
  - 분양가·청약·일정·계약 미공개 시 전부 **"입주자모집공고 시점 기준 추후 안내"** — 수치 창작 0. **분양가 공개 시 `Residence`+`AggregateOffer`(또는 `Offer`)** 생성, 미공개면 Offer 없이 `Residence` additionalProperty에 "분양가·청약·일정 = 추후 안내". '최저가/확정/직결' 단정·비교 금지.
- UI: **분양가·일정 미공개 시(흔함)** 아코디언 대신 **공급 개요 표 + 분양가/청약/일정/계약 "입주자모집공고 시점 기준 추후 안내" 섹션**(2×2 그리드 등). 공개 시 아코디언(보더 없이 `border-line` divider, 활성 시 브론즈 1px underline + 제목 굵기 500, 색만 의존 X). 표는 `LineSpec`로 통일, 값 16px+ ink.
- **분양 단계 전환(청약 종료 → 선착순)**: 정규 청약(특공·1·2순위·정당계약)이 **종료되면** 분양안내 페이지는 **선착순(잔여세대) 안내로 전환**한다. 운영자 설정 fact(`SUBSCRIPTION_CLOSED`/`SALES_PHASE` = content.ts)로 분기:
  - `SUBSCRIPTION_CLOSED=true` → **"정규 청약 종료 · 현재 선착순 분양 중"** 한 문장 + 선착순 장점(청약통장·순위 무관 동·호지정 계약 가능)만 간단히. **지나간 청약 일정(과거 날짜) 표는 노출하지 않는다** — 종료된 날짜 목록은 미분양 인식을 줌(반드시 제거).
  - `false`(청약 진행 중) → "청약 일정" 표(공고·특공·1·2순위·발표·계약) 노출, 접수 단계 안내.
  - ⚠️ **미분양 인식 주는 표현/요소 금지**: 지나간 청약 일정 표·"지난 청약 일정(종료)" 제목·"잔여세대·소진에 따라 변동" 반복 = 부정 인식 → 쓰지 말 것. "선착순 분양 중"으로 능동·긍정 프레이밍. 허위 마감·"완판/최저가/확정" 단정 금지, 분양가는 입주자모집공고 시점 표기 유지.
- **About**(eyebrow `About This Page`) `분양 한눈에 보기` 공급({N}세대 {전용면적}) / 분양가·청약 자격·분양 일정은 입주자모집공고 기준 추후(시점 표기 의무) / 분양 상담 {전화}·관심고객등록. ※"최저가/확정/직결" 단정·비교 금지. 권장 1,000~2,000자.
- **대표 비주얼(이미지 0 방지)**: 분양안내는 표(분양가·일정·공급개요) 중심이지만 **상단에 단지 대표 외관 1컷(풀와이드)** 을 둔다. 분양안내 전용 이미지 카테고리는 보통 없으므로(원본 분양가·청약은 PDF·표), **미사용 자산(예: 메인 히어로용 조감 CG 중 미사용분)을 `public/images/sales/`로 재활용** — 단, 다른 페이지에 쓰인 이미지와 **중복 금지(near-dup)**. 평면·조경·커뮤니티 컷은 넣지 않음(각 영역 페이지로). ※ `public/images/<cat>`는 4단계 큐레이션 카테고리별 생성이라 sales·news·faq 등 무(無)카테고리 페이지는 dir이 없을 수 있음 → 대표 비주얼 필요 시 위처럼 수동 보강.

## 모델하우스 `/modelhouse` (모티프 `gallery`)

- **TITLE** `{현장명} 모델하우스 | 방문예약·분양상담` (연결어 "및" 삭제 — 다른 페이지처럼 `·`로 통일) · **DESC**(≤80) `{현장명} 모델하우스 방문예약 안내. {모델하우스주소} 위치·관람 방법 확인 후 바로 예약하세요. 상담 {전화}.` (※"모델하우스" 명칭은 현장에 따라 갤러리·홍보관 등으로 교체)
- **H1** `{현장명} 모델하우스` · **H2** 방문예약(히어로 다음 2단 좌측 제목) · 모델하우스 위치안내(표) · **유닛·인테리어 갤러리**(견본주택 컷) · 관람 예약 방법(3 STEP) · 분양상담 신청 · 자주 묻는 질문(→ /faq). ※하단 관심고객등록 섹션 없음(전환은 상단 예약폼 + 고정 navbar CTA로 단일화).
- ★**표기 통일 = `모델하우스`(검색어) · `견본주택`은 첫 등장 1회 병기만**(정본 = [14단계 §검색어 표기 우선](일-14단계-SEO-GEO.md)): 네이버에 치는 말은 `모델하우스`인데 원본이 입주자모집공고라 본문이 `견본주택`으로 채워지는 실수가 잦다(실측: 우리 견본주택 9회·모델하우스 6회 ↔ 1위 사이트 견본주택 0회·모델하우스 22회 = **이 페이지가 지는 주된 이유**). 본문 기본은 `모델하우스`, 좌측 소개 첫 줄에서 `모델하우스(견본주택)`로 1회만 병기, **모집공고를 직접 인용하는 문장·표·※주석은 원문 용어 그대로**. ⚠️ **단어 치환일 뿐 분량은 그대로** — 문장 추가 0(밀도 작업 아님). 적용 범위는 이 페이지 + 메인 관심고객등록 📍라벨 + FAQ 답변 + `/sales`·`/floorplan` 언급까지. ※현장이 실제로 `갤러리·홍보관` 등을 쓰면 그 명칭으로 교체.
- **유닛·인테리어 갤러리(이미지 최대 활용)**: modelhouse category로 keep된 견본주택 컷을 **전부** 유닛별(예: 84A·84B 등)로 묶어 그리드/풀와이드로 노출. 세로 인테리어 컷은 무크롭. 캡션은 검증된 유닛명만(연출용 가구·유상옵션 ※주석).
- ★★**`<img>` 0개로 배포 금지 (blocker)**: 이 페이지에 실제 `<img>` 태그가 하나도 없으면 **네이버 이미지 검색 노출 자격 자체가 사라진다.** 네이버 웹마스터 가이드 [사이트 최적화](https://searchadvisor.naver.com/guide/report-seo): *"문서내 이미지의 경우도 alt 속성을 활용하여 이미지를 대체할 수 있는 텍스트로 표현할 수 있습니다. 이 정보는 검색엔진이 해당 이미지를 이해하는데 도움을 줄 수 있으며 **이미지 검색결과에 활용할 수 있는 매우 중요한 정보**로 간주합니다."*
  - "{현장명} 모델하우스"는 **통합검색에 이미지 영역이 함께 뜨는 쿼리**다. 이미지가 0개면 그 영역 후보에 오르지 못한다 — 웹사이트 순위와 별개로 **유입 경로 하나가 통째로 닫힌 상태**다. (실측에서 확인된 누락 패턴: 갤러리 섹션은 설계돼 있는데 이미지 자산이 연결되지 않아 렌더 결과 `<img>` 0개)
  - ⚠️ **순위 개선 항목이 아니다** — 이건 "노출 자격을 만드는" 작업이지 웹사이트 검색 순위를 올리는 작업이 아니다. 기대치를 그렇게 잡을 것.
  - 자산이 부족하면 **최소 1컷이라도** 실물 견본주택/유닛 사진을 넣는다. **없는 이미지를 다른 페이지에서 빌려 오지 말 것**(near-duplicate·소유 규칙 위반) — 부족하면 부족한 대로 두고 [분류표](이미지-배치표.md)에 "자산 없음"으로 기록한다.
- ⚠️ **E-모델하우스/360° VR은 실제 자산(VR 투어·파노라마)이 확인될 때만 표기·링크·임베드**. 자산이 없으면 "E-모델하우스/360° VR" 문구 자체를 넣지 말 것(없는 기능 안내 = 허위). VR 자산이 없는 현장은 견본주택 인테리어 갤러리만 노출.
  - ★**VR은 위 `<img>` 요건을 대체하지 못한다** — VR 임베드는 `iframe`이라 검색엔진의 이미지 수집 대상이 아니다. VR을 넣더라도 **진입 썸네일을 실제 `<img>`로 깔고**(alt = 유닛명 등 자연어) 그 위에 재생 버튼을 얹는 구조로 만들어야 이미지 검색에 잡힌다. VR만 있고 `<img>` 0개면 위 blocker에 그대로 걸린다.
- **페이지 구성 = 히어로 다음 2단 레이아웃**(참조 스크린샷 정본 · 별도 인트로 없음 = 좌측 소개가 인트로):
  - **좌 — Modelhouse Information**(영문 eyebrow): 제목(H2 `{현장명} 모델하우스`) + `모델하우스(견본주택) · {주소}` + **소개 문단**(현장에서 평면·커뮤니티·특화를 직접 경험 → 온라인 방문예약 신청 권유, 따뜻한 톤) + 📍**위치 안내**({주소} + "방문 시 사전 예약 권장") + 📞**방문·분양 문의**(큰 serif {전화} + "선예약 후 방문 안내 · 동·호/잔여 현황 상담"). ⚠️ **길찾기 버튼(`MapLinks`) 미사용** — 위치는 `모델하우스(견본주택) · {주소}` 텍스트 + 아래 위치안내 표로 안내하고, 주소·전화는 아래 `LocalBusiness`/`RealEstateAgent` JSON-LD로 구조화 노출(외부 지도앱 이탈 방지 — 방문 동선은 예약 폼으로 수렴).
  - ★**방문예약 혜택 ✓4불릿**(소개 문단과 📍위치 사이 배치, 브론즈 ✓): **이 폼이 전 표면 CTA의 종착**(헤더·히어로·콜바·서브 PageOutro 모두 `/modelhouse#reservation`로 수렴)이고 앵커 직행 시 갤러리·3STEP 등 설득 컨텍스트를 건너뛰고 착지하므로, 좌측을 혜택 불릿으로 채워 **"왜 지금 방문예약 하나"를 폼 옆에서 설득 + 좌측 높이로 횡(중간 공백) 방지**. ⚠️ **메인 관심고객등록 혜택과 다른 워딩 필수**(같은 `ContactForm` 재사용 → near-duplicate 금지): 메인 = "등록 시 **받는 정보**"(분양가·잔여 세대 우선 안내) 톤 ↔ 여기 = "방문 시 **직접 경험·확인**하는 것" 톤. 예시: `실물 모델하우스에서 평면·공간 구성 직접 확인` / `커뮤니티·특화 설계 현장 체험` / `동·호 지정·잔여 세대 현황 즉시 상담` / `1:1 맞춤 분양 상담 + 방문 일정 조율`. 검증 fact 기반·과장 수식어 0.
  - **우 — Reservation Form**(영문 eyebrow): **eyebrow `Reservation Form` + 제목 `방문예약 신청` + 안내문 `담당자가 확인 후 연락드려 방문 일정을 확정합니다`** 를 폼 상단에 두고, 그 아래 폼(성함·연락처·연락 가능한 시간대·개인정보 동의·제출, 폼 규정 = 8단계). 폼 컴포넌트에 `eyebrow/title/note` prop으로 주입(메인 관심고객등록 폼도 동일 패턴 재사용). ⚠️ **앵커는 이 폼에 직접 부여**: `id="reservation"`+`scroll-mt-24`는 2단 래퍼·좌측 컬럼이 아니라 **우측 폼 컴포넌트(eyebrow/title/inputs 묶음)** 에 건다. 모바일(2단 세로 스택)에서 헤더·히어로·콜바·PageOutro 어느 CTA를 눌러도 좌측 정보가 아니라 **폼 입력부로 직행**해야 하며, `scroll-mt-24`로 고정 헤더 아래에 폼 제목+첫 입력칸이 보이게 착지(전 브레이크포인트 헤더 높이 보정). ❌ 2단 섹션/좌측 컬럼에 `id` 부여 금지 = '폼 윗부분 착지' 버그.
  - **좌우 영문 eyebrow 대칭**: 좌 `Modelhouse Information` ↔ 우 `Reservation Form`(브론즈 accent italic). ⚠️ **'GRAND OPEN {날짜}' 등 오픈일은 1차 출처로 확정된 경우만**; 미확정·이미 선착순 단계면 날짜 창작 금지 → "방문 시 사전 예약 권장"으로 대체.
  - 이후: 위치안내 표(갤러리·현장·분양상담) → 관람 예약 방법 **3 STEP** → 분양상담 → FAQ 링크 → About.
- ★**하단 관심고객등록 섹션 = 두지 않는다(중복 제거 · 전환 단일화)**: 모델하우스 페이지 하단에 별도 `관심고객등록` 섹션(eyebrow `Register` + H2 `관심고객등록` + 혜택 ✓4 + 견본주택·문의)을 **만들지 않는다**. 이유: ① 상단 2단에 이미 **방문예약 폼(#reservation · 폼 1개)** 이 있고, ② **고정 헤더(navbar)의 관심등록/상담 CTA가 스크롤 내내 상시 노출**되며 동일하게 `#reservation`로 수렴하므로, 하단까지 내려온 방문자도 navbar 버튼으로 즉시 전환할 수 있다. 하단에 또 `관심고객등록` 섹션을 두면 **폼 1개에 헤더가 「방문예약 신청」·「관심고객등록」 2개 + 두 이름을 붙인 CTA(예: "방문예약·관심고객 등록하기")**가 되어 자기중복 + [10단계] 메인 관심고객등록과 near-duplicate만 만든다.
  - **두꺼운 관심고객등록 블록은 [10단계](일-10단계-메인페이지.md) 메인 페이지 1곳에만.** 모델하우스는 **상단 방문예약 폼 + 상시 navbar CTA**로 전환을 단일화한다(별도 하단 CTA 띠·폼·api 신설 0 · [G12-FORM] 앵커 1개 `#reservation` 유지).
  - **폼 안내문(note)로 겸용 명시(권장)**: 상단 우측 폼 note에 "방문예약 겸 관심고객등록 — 방문이 어려우면 연락처만 남겨도 분양 상담드립니다"류 한 줄을 더해, '방문 의향자'와 '정보부터 받고 싶은 방문자'를 폼 하나로 함께 수렴한다(메인 관심고객등록과 워딩 중복 0).
  - ⚠️ 갤러리·3STEP 다음은 곧바로 **분양상담 → FAQ 링크 → About**(중간에 관심고객등록 섹션 삽입 금지). 선착순/청약 분기·GRAND OPEN 날짜 창작 금지·길찾기 버튼(`MapLinks`) 미사용은 [10단계 §관심고객등록 섹션](일-10단계-메인페이지.md)과 동일(잔여 세대/선착순 긍정 프레이밍).
- ★**모델하우스(견본주택) 운영(관람) 시간 = 10:00~18:00**(분양 견본주택 **범용 기본값**, `content.ts` `modelHouse.hours`). 현장이 별도 시간을 고지하면 그 값으로 교체하되, 없으면 이 10~18시를 쓴다.
  - ⚠️ **혼동 금지(과거 실수)**: 입주자모집공고·계약안내의 **계약/유상옵션 방문 시간(예: 10:00~17:00)**을 견본주택 **관람 시간**으로 가져오지 말 것. 둘은 별개다(계약 이벤트 시간 ≠ 상시 관람 시간). 관람 시간은 위 범용 기본 또는 현장 고지값.
- **JSON-LD** = `LocalBusiness` 또는 `RealEstateAgent` — **주소 공개 시 `address`(PostalAddress)·`telephone` 필수 포함**(가능하면 `geo`), 운영시간 확정 시 `openingHoursSpecification`(미확정이면 생략). 페이지에 보이는 주소·전화와 **1:1 일치**(미노출 정보 schema 단독 표기 금지). 주소 미공개면 `LocalBusiness` 생성 금지(창작 0). **NAP(이름·주소·전화) 구조화 데이터는 이 페이지 1곳에만**(다른 페이지 복제 금지 — 중복 schema 방지).
- 일정·주소·연락처는 핵심 정보(17px+ ink 500).
- **About**(eyebrow `About This Page`) `모델하우스 한눈에 보기` 모델하우스({모델하우스주소}·오픈 예정) / 방문예약 6단계 / 분양 상담 {전화} 2문단. 권장 1,000~2,000자.

## 완료 조건 (사람용 요약)

> ※ 게이트가 자동 판정하는 항목은 여기서 뺐다(머리말 `이 단계 blocker` 줄 + `npm run verify`가 본다). 아래는 **기계가 못 잡는 것만** 사람이 확인한다.


- [ ] 2페이지 TITLE/DESC/H1/H2/About 적용, `{플레이스홀더}` 0
- [ ] **제외 규칙 준수**: 분양안내에 **인지세·부동산 거래신고(신고기한·신고필증) 문단·표·이미지 0**(모집공고·계약안내에 있어도 옮기지 않음 — 관련 이미지는 분류표에 '미사용 — 분양안내 제외 규칙'으로 기록)
- [ ] 분양가 미공개 시 `Offer` 미생성 + "추후 안내" 표기, 단정·비교 표현 0
- [ ] 모델하우스 2단 레이아웃 + 위치·전화 텍스트(길찾기 버튼 0) + 예약 폼 + `id="reservation"`(**우측 폼 컴포넌트에 직접** · `scroll-mt-24`) — 모바일 2단 세로 스택 시 좌측 정보 아닌 **폼 입력부로 착지**
- [ ] **관심고객/방문예약 라인 카피**: 좌 소개문단(현장 경험→방문예약 권유)·**방문예약 혜택 ✓4불릿(메인 관심고객등록과 다른 워딩 — "방문 시 직접 경험·확인" 톤, near-duplicate 0)**·위치(사전예약 권장)·전화(선예약 안내) + 우 폼 `Reservation Form` eyebrow·`방문예약 신청` 제목·확인 연락 안내문. 좌우 영문 eyebrow 대칭. 오픈일 미확정 시 날짜 창작 0 + **하단 관심고객등록 섹션 미생성(제거) — 두꺼운 관심고객등록 블록은 메인 1곳에만 · 모델하우스 하단 전환은 고정 navbar CTA + 상단 #reservation 폼으로 단일화 · 별도 하단 CTA 띠/폼/api 신설 0 · 갤러리·3STEP → 분양상담 → FAQ → About 순서로 중간 관심고객등록 삽입 0**
- [ ] **이미지 최대 활용**: modelhouse category keep 컷 전부 갤러리 배치(미사용분 사유 명시), 세로 인테리어 컷 무크롭(글자/구도 잘림 0), 캡션 명칭 OCR 검증값만(창작 0)
- [ ] **fact 전사([3단계 ⑦ 전사 대장](일-3단계-검증보강.md))**: 유닛/평형 명칭·구성·수치가 이미지에만 박혀 있으면 **HTML 텍스트(캡션+본문)로 전사** — 무크롭 인테리어 컷은 시각용, 그 안 fact는 alt로 대체 금지·본문 텍스트 병기(이미지-only 방치 0, OCR 검증값만)
- [ ] ★**`/modelhouse` 렌더 결과에 `<img>` ≥1 (blocker)**: view-source/빌드 산출물에서 실제 `<img>` 태그 수를 센다(CSS background-image·`iframe` VR은 **불인정**) · 전 컷 `alt` 자연어 채움(빈 `alt=""` 0) · VR 채택 시 진입 썸네일이 `<img>`인지 확인 · 자산 부족으로 컷이 적으면 [분류표](이미지-배치표.md)에 "자산 없음" 기록. ※이미지 검색 **노출 자격** 항목이지 순위 항목이 아님
- [ ] 모티프 doc/gallery, 핵심 정보 17px+ ink 500
- [ ] **내부링크 전환 동선([11단계 §내부링크 시스템](일-11단계-서브-단지정보.md) 정본)**: 본문 맥락 링크 ≤2/페이지·같은 URL 1회·서술형 앵커(/sales → 평면도 또는 모델하우스, /modelhouse → 분양안내 등 퍼널 방향), About 링크 0, 하단 전환은 예약폼·CTA로, orphan 0·도배 0
- [ ] (모델하우스 주소 공개 시) `LocalBusiness`/`RealEstateAgent` JSON-LD 생성 — `address`(PostalAddress)·`telephone` **필수 포함**(운영시간 확정 시 `openingHoursSpecification`), 페이지 노출값과 1:1 / 미공개 시 미생성(창작 금지) / NAP은 이 페이지 1곳에만(복제 0)
- [ ] **본문 충실화([6단계](일-6단계-전역규칙.md) §F)**: 각 About 1,000~2,000자 **측정 충족** + 공급개요 표·내부링크, 어뷰징 0(‘최저가/확정/직결’ 단정·비교 금지 유지)

---

## 완료조건 (기계 판정)


- [G12-FORM] 모델하우스: 예약 anchor(#reservation)·처리방침 링크·api/contact route(백엔드 구현 정본 = [9단계](일-9단계-관심고객등록.md)) | auto | blocker
- [G12-SCHEDULE] sales·modelhouse 지난 청약 일정 나열 0·미분양 urgency 0 | auto | blocker
- [G12-TERM] `/modelhouse` 본문 표기: `모델하우스` 등장수 ≥ `견본주택` × 2 (첫 등장 1회 병기는 통과 · 위 ★표기 통일 규칙의 자동 강제) | auto | blocker