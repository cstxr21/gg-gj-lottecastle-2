> 🔒 **완료 게이트**: 이 단계를 "완료"라 말하기 전 `npm run verify -- --stage=13` 가 **전부 GREEN**임을 출력으로 증명한다(게이트 규약·auto/manual·필터 주의 = [0-시작하기](일-0-시작하기.md) §게이트).

# 13단계 — 서브페이지: 분양소식 · FAQ

> **작업 순서 13/16** · 전제: 5·6·7·8단계 · 자기완결(IA 각 페이지 + UI §2.9~2.10 원문 포함)
> **읽을 것(착수 전)**: [3단계](일-3단계-검증보강.md) ⑧ 보도 대장 · [6단계](일-6단계-전역규칙.md) §C·§F · [8단계](일-8단계-공통컴포넌트.md) §9 `SourceNote` · [11단계](일-11단계-서브-단지정보.md) §내부링크 시스템
> **대상 2개**: 분양소식 `/news`(+ `/news/[slug]`) · FAQ `/faq`
> **공통 전제**: 미확정 정보 "입주자모집공고 기준 추후 안내", 보도 근거는 **하단 ※ 출처 주석·매체 크레딧**으로만(본문 문장 안 "보도에 따르면·알려졌다"류 출처 자백 금지 — [6단계](일-6단계-전역규칙.md) §C).
> ⚠️ **현장명 본문 밀도(대명사 환원)** = [11단계 §공통 원칙](일-11단계-서브-단지정보.md) 동일 적용 — 특히 **FAQ 답변**이 "본 단지는"으로 브랜드를 먹기 쉬운 지점이다(질문에 현장명이 있으면 답변도 현장명으로 받는다). 진단선 250자당 1회, **횟수 채우기 0**. `/news`는 글마다 제목·리드에 현장명이 들어가 대개 이미 충족 — 억지 보강 대상 아님. 정본 = [14단계 §브랜드 토큰 본문 밀도](일-14단계-SEO-GEO.md).
> **글 발행 자체는 15단계 반복** — 여기선 두 페이지의 구조·스키마를 만든다.
> ⚠️ **[필수 게이트] 적용**(이미지/보도 자산 있을 때): [11단계 §공통 원칙 게이트](일-11단계-서브-단지정보.md#공통-원칙--본문-밀도이미지-활용-thin-페이지-금지) 동일 — 사용 자산 **전수 확인** → **[분류표](이미지-배치표.md) 기록** → 완료조건 ✅ 증명.
> ⚠️ **내부링크 시스템 = [11단계 §내부링크 시스템(심플)](일-11단계-서브-단지정보.md) 적용**: **카드 블록·매트릭스 없음**(전역 nav가 상호연결). 본문 맥락 링크 **≤2/페이지**·동일 URL 1회·**About 링크 0**·하단 CTA 1개·**BreadcrumbList JSON-LD**·orphan 0. (FAQ는 FAQPage, news 보도 출처 링크는 별개.)
> **배포 점검(16단계)**: /news·/faq 스키마와 robots/sitemap/llms 반영은 배포 직전 **[16단계](일-16단계-배포점검.md)**가 회귀 점검한다(누락 = blocker).
> **이 단계 blocker**: 없음 — 권고만 〔권고 4개는 진행을 막지 않는다〕

---

## 페이지별 섹션 → 콘텐츠 매핑 + 구성 순서 (영역 적합성)

| 페이지 | 구성 순서(섹션) | 들어가면 안 되는 것 |
|---|---|---|
| **분양소식 `/news`** | 히어로 → 최신 인트로 → 발행 글 카드(매거진 2단) → 언론 보도 아카이브(언론사별 H3) → About | 분양가/평면 등 단지 fact 상세(→해당 서브, 요약+링크) |
| **FAQ `/faq`** | 히어로 → 카테고리(H2) 아코디언(질문 H3/summary) → About | 같은 Q를 서브페이지에 복제(중복 스키마 금지) |

> ⚠️ **이미지**: /news 발행 글 카드 썸네일은 **실제 비율 무크롭**·`width/height`·alt(제목+매체), 장식 이미지 `alt=""`. **창작 금지**: 보도는 **출처(언론사·날짜·원문)를 카드 크레딧·하단 ※ 주석에 명시**(본문 문장 안 출처 자백 금지), 미확정 수치는 "입주자모집공고 기준 추후 안내"(임의 수치·인용 0). **near-duplicate 금지**: 같은 Q·동일 FAQPage 스키마를 여러 URL에 복제하지 말 것(FAQ는 `/faq` 1곳).

> **내부링크 적용([11단계 §내부링크 시스템](일-11단계-서브-단지정보.md) 정본 그대로 — 재정의 금지)**: /faq 답변 속 주제 페이지 앵커(분양가→분양안내, 평면→평면도, 방문→모델하우스)가 이 페이지의 본문 맥락 링크(≤2·같은 URL 1회 준수, 답변마다 걸지 말 것). 발행 글 상세(`/news/[slug]`)는 본문 말미 관련 서브 1~2개 + 하단 CTA. **About 블록은 링크 0**(순수 요약 — "/news About에 링크" 구 사양 폐기).

---

## 분양소식 `/news` (모티프 `news`)

- **TITLE** `{현장명} 분양소식 | 부동산 동향` (`{현장명} 사업개요`와 "공급정보" 중복돼 삭제) · **DESC**(≤80) `{현장명} 분양소식. 최신 공급정보, 입지분석, 부동산 시장동향과 언론사 보도자료를 한눈에 확인하세요.`
- **H1** `{현장명} 분양소식` · **H2** 최신 공급정보(인트로) · 발행 글 · {현장명} 관련 보도자료(언론사별).
- ★**게재 수량 = 전량**: `/news`는 **발행 글 전부(최대 7편) + 보도 아카이브 전부**를 싣는다. 메인은 요약(발행글 3편 + 보도 `min(4, 전체의 절반)` — [10단계](일-10단계-메인페이지.md))이고 **전량은 여기가 소유**한다. 보도는 상한이 없다 — 매체·날짜·헤드라인 + 원문 링크라 thin 위험이 없고 많을수록 신뢰 신호가 는다(언론사별로 묶어 최신순).
- **색인 페이지**: 최신 분양정보 인트로 → **발행 글 카드**(카테고리·날짜·제목·발췌, 매거진 2단) → **언론 보도자료 아카이브**(언론사별 H3 + 외부 원문 링크 `target=_blank rel=noopener`) → 운영 방향/About.
- **발행 글 상세 `/news/[slug]`**: 에디토리얼 헤더(breadcrumb·카테고리·날짜·H1·리드) → H2 본문 섹션 → 출처 주석 → 관련 페이지 링크. **`NewsArticle` JSON-LD**(headline·datePublished·dateModified·author·publisher·mainEntityOfPage).
- 데이터 `lib/news.ts`(`POSTS` 발행 글 + `PRESS` 보도 아카이브), `generateStaticParams`로 글 추가 시 자동 라우트, sitemap에 글 포함. ⚠️ **RSS 피드 라우트(`app/rss.xml`)는 만들지 않는다** — 7편에서 멈추는 사이트라 피드는 실익이 없다(자주 갱신되는 사이트용). 색인은 `sitemap.xml` + 네이버 서치어드바이저 "웹페이지 수집요청"으로 처리한다(15단계 §0).
- ⚠️ **샘플·더미 글 발행 금지**: 13단계에서는 `POSTS`를 **빈 배열 `[]`**로 둔다(샘플·예시·더미 글 **0편**). `/news` 색인·카드 레이아웃·`NewsArticle` 템플릿·JSON-LD 구조는 빈 `POSTS`로 검증을 통과한다(`[G13-NEWS]`가 **색인 구조만** 강제). 실제 글은 **[15-A] 지역 롱테일 2~4편**으로 첫 배포 전에 채운다. 여기서 샘플을 넣으면 15-A분과 **합쳐져 과발행**된다.
- 보도 근거는 **하단 ※ 출처 주석(언론사·날짜·원문)** · 본문 문장은 검증된 사실로 직접 서술("보도에 따르면·알려졌다·전해졌다" 0 — [6단계](일-6단계-전역규칙.md) §C) · 미확정 정보는 "입주자모집공고 기준 추후 안내".
- **About** `{현장명} 분양소식 한눈에 보기` 공급정보·입지분석·시장동향·보도자료 지속 업데이트 / 분양가·일정 추후.
- **권장 텍스트 수·이미지**: 색인 인트로 2~3문장 + About 600~1,200자. 발행 글 상세(`/news/[slug]`)는 H2 2~3개·본문 800자+. **발행 글 카드 썸네일 이미지 활용**(alt=제목+매체, width/height 명시). 보도 아카이브 항목은 매체·날짜·헤드라인·1줄 요약(near-duplicate 금지).
- **운영 목표** **현장당 7편(상한)** — 지역 롱테일 4(15-A · 첫 배포 전) + 현장 사건 뉴스 3(15-B · 사건 발생 시에만). ⚠️ **`{현장명}+X` 롱테일 슬롯은 폐지**(그 fact는 분양소식이 아니라 해당 서브페이지가 소유). 정본 = 15단계 §0.

## FAQ `/faq` (모티프 `qa`)

- **TITLE** `{현장명} FAQ | 자주 묻는 질문` · **DESC**(≤80) `{현장명} 자주 묻는 질문. 분양, 방문예약, 계약·입주, 주차·이용 등 궁금한 점을 확인하세요.`
- **H1** `{현장명} FAQ` · **H2**(카테고리별 아코디언 + FAQPage JSON-LD) 분양 관련 질문 · 방문예약 안내 · 계약 및 입주 안내 · 주차 및 이용 안내 · 기타 문의사항.
- UI: **카테고리별(H2) 아코디언**(`<details>`/`<summary>` — JS 없이 콘텐츠가 정적 HTML 노출 → 네이버 Yeti·AI 추출 가능). `+`→`×` 회전 표시.
- **사이트 FAQ 허브 — `FAQPage` JSON-LD는 이 페이지에만**(서브페이지 FAQ 섹션·스키마 복제 금지 = 중복 방지). `dateModified` 포함.
- 분양가·일정 등 미확정 답변은 임의 수치 금지("입주자모집공고 기준 추후 안내"). 인접·동일부지 내 별개 단지가 있으면 본 단지와 구분 명시.
- **권장 텍스트 수·구조(H2/H3 최대 활용)**: 카테고리(H2) 4~6개 + 질문 총 12개+(페이지 고유 질문, 같은 Q 여러 페이지 복제 0), 답변 각 1~3문장 검증 fact. 분량은 정보 밀도 우선(개수 맞추기용 억지 답변=어뷰징). 답이 "추후 안내"뿐인 질문으로 개수 채우지 말 것.
  - **fact 전사**: 답변에 담을 확정 fact(면적·세대·시설·위치·특별공급 등)는 **텍스트로 직접 서술** — "자세한 건 이미지/평면도 참조"식 회피 금지. 확정값은 [3단계 전사 대장](일-3단계-검증보강.md)·content.ts 검증값에서, 미확정은 "추후 안내".
- **About** `{현장명} 자주 묻는 질문 한눈에 보기` 분양·방문예약·계약/입주·주차/이용 / 미정은 입주자모집공고 기준 추후, 추가 문의 {전화} 1~2문단(400~800자).

## 완료 조건 (사람용 요약)

> ※ 게이트가 자동 판정하는 항목은 여기서 뺐다(머리말 `이 단계 blocker` 줄 + `npm run verify`가 본다). 아래는 **기계가 못 잡는 것만** 사람이 확인한다.


- [ ] 2페이지 TITLE/DESC/H1/H2/About 적용, `{플레이스홀더}` 0
- [ ] `/news` 색인 + `/news/[slug]` NewsArticle JSON-LD + `lib/news.ts`(POSTS/PRESS) + generateStaticParams + sitemap
- [ ] 모티프 news/qa, 보도 근거는 하단 ※ 출처 주석·매체 크레딧으로(본문 문장 안 출처 자백 0)
- [ ] **내부링크([11단계 정본] 준수)**: /faq 답변 주제 앵커·글 상세 관련 서브 링크 = 본문 맥락 링크 ≤2·같은 URL 1회·서술형, About 링크 0, orphan 0·도배 0
- [ ] /news 발행 글 카드 썸네일 이미지(alt=제목+매체·width/height) 활용, 장식 이미지 alt=""
- [ ] **본문 충실화([6단계](일-6단계-전역규칙.md) §F)**: /news About + 보도 아카이브(출처별) 충실 / /faq 카테고리별 답변 충실(임의 수치 0) / 글 상한 7편(15단계 §0) — 어뷰징 0(개수 채우기·near-duplicate 금지)

---

## 완료조건 (기계 판정)


- [G13-NEWS] /news 색인 구조 존재(POSTS 빈 배열 허용 = 13단계 샘플 0편)·**rss.xml route 없음**(15단계 7편 체제 — RSS 미사용)·발행글 있으면 NewsArticle JSON-LD | auto | 권고
- [G13-FAQ-DEDUP] FAQPage 스키마 = /faq 단독(서브 중복 0) | auto | 권고
- [G13-CONTENT] /news·/faq 권장 분량(발행글 800자+·FAQ 12문항+ 등) 충족 | manual | 권고
- [G13-VOLUME] /faq FAQPage Question ≥12개 · /news/[slug] 발행글 전부 본문 800자+(글 간 공통 헤더·푸터 제외 기준) (G13-CONTENT의 기계 판정 가능분 — 내용 충실성은 G13-CONTENT가 계속 담당) | auto | 권고